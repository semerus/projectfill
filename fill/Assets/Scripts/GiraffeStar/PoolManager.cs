using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PoolManager {
    /*
    static GameObject poolRoot;
    static Dictionary<string, List<GameObject>> loadPool = new Dictionary<string, List<GameObject>>();
    static Dictionary<string, List<GameObject>> pool = new Dictionary<string, List<GameObject>>();

    public static GameObject Spawn(GameObject go)
    {
        if(poolRoot == null)
        {
            poolRoot = new GameObject("PoolContainer");
            poolRoot.transform.position = Vector3.zero;
        }


    }

    public static void Despawn(GameObject go)
    {
        if (poolRoot == null)
        {
            poolRoot = new GameObject("PoolContainer");
            poolRoot.transform.position = Vector3.zero;
        }

        go.transform.SetParent(poolRoot.transform);

        var tag = go.GetComponent<PoolTag>();
        if(tag != null)
    }
    */
}
